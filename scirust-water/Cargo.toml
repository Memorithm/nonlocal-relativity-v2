[package]
name = "scirust-water"
version = "0.1.0"
edition = "2021"
publish = false
description = "SciRust water networks — deterministic acoustic leak localization and water-hammer transient analysis"

[dependencies]
serde = { version = "1", features = ["derive"] }
